<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.MangoAndroidChallenge" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash screen theme (used as the activity's starting theme). -->
    <style name="Theme.MangoAndroidChallenge.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.MangoAndroidChallenge</item>
    </style>

</resources>
